import React from "react";
import "./Mask.css"; // or inline styles
import { height } from "@mui/system";

function Mask({size}) {
    return (
        <div
          className="masked-image"
        />
      );
}

export default Mask;
